{
  "name": "dryvia-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r public ../public",
    "vercel-build": "cd frontend && npm install && npm run build && cp -r public ../public"
  }
}
